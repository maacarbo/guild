# Plan Reorganisation — 2026-07-30

## Rulings

**Team evolution on compose:** YES — fully feasible, unanimous across all six analysts and confirmed by refutation. Every current-M4 mechanic is Multica REST calls, registry data, or conductor application code: hiring = agent/squad registry writes (or idle-pool selection), execution = the existing daemon forking per-task CLI subprocesses (verified-in-code, substrate-agnostic; upstream daemon is host-only by design — no Helm daemon template exists, so K8s is the substrate-EXOTIC path), role templates and role memory = pure Guild PG/brief data, budgets = gateway-native max_budget keys minted per engagement regardless of who was hired. The acceptance bar names no cluster object. Amendments honored: (a) the idle-pool fallback's 'known-supported mechanics' is overstated — it avoids the unverified agent-CREATION surface but its assignment path rests on the same M1a core-loop probes; (b) multi-daemon runtime registration is inference-from-code (per-task RuntimeID binding is what's verified), now an explicit M1a probe; (c) daemon-internal task parallelism is unverified in all frozen evidence — but it is a property of the Go binary, identical on K8s, so it cannot justify any sequencing change; pre-declared fallback: one compose daemon service per busy role. All residual risks are substrate-neutral; none is a compose limitation.

**Swap M3/M4:** YES — swap. Ruled milestone map (resolves the pool's one cardinality conflict in favor of the 5-milestone map used by four of five planners): M3 = Team evolution on the Tier 1 compose stack, shipping v0.2.0 = the feature-complete product (all three PRODUCT.md success criteria); M4 = Deployment options (optional, last), generic K8s conditional on recorded need. Grounds: (1) old-M3's own preamble — 'Nothing here adds a feature; everything adds an operational quality' — while success criterion 3 (mid-run hire) is part of the product definition; the directive forbids a feature-bearing milestone sitting behind a feature-free one. (2) Dependency-clean both directions, verified: old-M4's only contingency is the M1a probe; old-M3 feeds nothing old-M4 consumes. (3) Efficiency: the lift re-validates the capability matrix's transport rows once, against the FINAL post-team-evolution API surface. The sequencer's extra DX/v1.0.0 milestone is not adopted as a milestone; its content is folded into M1a/M1b/M2/M3 acceptance bars (ease-of-setup bars, doctor/smoke/demo/reset, fresh-machine tests) per the DX engineer's own leaner proposal. Rescued from the old K8s milestone so nothing is hostage to a maybe-never milestone: daemon-image publication → M2 ship gate; compose pg_dump backup → standing rules from M2; external-datastore exercise → M4 unconditional item. Watchdog retag ruling: prose says 'M2b watchdog'; PRODUCT success criterion 2 tags '(M2)'.

**K8s needed by requirements:** NO — zero requirements map to Kubernetes; confirmed by full trace and refutation. All seven PRODUCT.md flows, all four ROADMAP acceptance bars, and all three success criteria run on the compose stack: the only K8s mechanism in any flow is the validator's Job driver, explicitly one of two drivers with the same contract (docker-run is first-class). Delivered apps need only git hosting + a test-running container (ContractCheck is a closed command|artifact union — no deploy check kind exists). What K8s buys is three operational qualities, none a requirement at any tier: stronger sandbox/egress (deploy/README already classifies these 'recommended where supported, never assumed — documented residual risk'), GitOps reproducibility ('required for nobody'), and storage/backup discipline (achievable as compose cron). Availability cannot mandate it either: solo-operator product, crash-tolerance is application-level (dispatch saga + reconciliation-as-truth). Therefore K8s becomes the conditional final milestone (M4), generic-only, entered on recorded demand; all Talos/Cilium/NFS/Flux/ESO/homelab material exits the product docs into a personal runbook (docs/runbooks/authors-cluster.md, CREATE'd in the edit list). Inverse-audit findings fixed in the same MR: the budget kill-switch's FIVE stale M3 labels (it ships in M2b), OVERVIEW's K8s-Deployment component rows, the GitHub milestone object #3 title 'M3 — Budget enforcement + Kubernetes', and two silent Tier-1 gaps now written down as D6 decisions: scoped docker-socket proxy (never raw socket) and the documented validator-egress deviation.

# Guild reorganisation — synthesis report (2026-07-30)

## Rulings on the three questions

1. **Kubernetes needed by requirements? NO.** All 7 PRODUCT.md flows, all acceptance bars, all 3 success criteria trace to compose-runnable infrastructure. Only K8s mechanism anywhere: the validator Job driver — explicitly one of "two drivers with the same contract" (ARCHITECTURE D6), docker-run first-class. Old M3's own preamble: "Nothing here adds a feature." Delivered apps = git repo + test-running container (ContractCheck is command|artifact only). K8s buys 3 operational qualities, all already classified "recommended, never assumed" in deploy/README Tier 2.
2. **Team evolution on compose? YES** — every bullet is REST + registry data + conductor code; upstream daemon is host-only by design (no Helm daemon template), so K8s is the substrate-exotic path. Amendments honored: idle-pool "known-supported" softened (assignment path is M1a-probed); multi-daemon registration downgraded to inference + M1a probe; daemon-internal parallelism flagged unverified + M1a probe (substrate-neutral either way).
3. **Swap M3/M4? YES** — dependency-clean both directions (verified: old-M4's only contingency is the M1a probe; old-M3 feeds nothing old-M4 consumes), unanimous across all six analysts + refutation, and the only ordering consistent with the directive.

## Ruled conflicts (the three the refutation flagged)

- **Milestone cardinality**: 5-milestone map adopted (M3 = team evolution/v0.2.0, M4 = deployment options). The sequencer's extra DX/v1.0.0 milestone is folded into existing acceptance bars (doctor/smoke/demo/reset, fresh-machine ≤30 min bar, two-credential bootstrap) rather than added as a milestone.
- **Hostage deliverables**: daemon-image publication → **M2 ship gate**; compose pg_dump backup → **standing rules from M2**; external-datastore exercise → **M4 unconditional item** (runs even if the K8s conditional doesn't).
- **Watchdog retag**: prose = "M2b watchdog"; PRODUCT success criterion 2 = "(M2)". All FIVE stale locations fixed (PRODUCT ×3, OVERVIEW F7, ARCHITECTURE lifecycle).

## What moved where (relocation, never deletion)

- **CREATE `docs/runbooks/authors-cluster.md`** (non-normative; recommend eventual move to the home-lab repo): Tier 3 section, Cilium toFQDNs + DNS-proxy, gVisor-on-Talos plan (verbatim), NFS specifics (verbatim), GitOps/Flux promotion bullet + acceptance (verbatim), OQ6 + OQ7 + OQ5's placement clause, the dbsrv01/Ollama/shared-LiteLLM exclusion list. deploy/README keeps a single footnote pointer.
- **Stays in product docs as generic requirements**: FQDN egress (with CIDR-forbidden rationale), RuntimeClass sandboxing, SA/PSA hygiene, normative secrets table, generic storage rules, D2 supply-chain, D6 trust model — all untouched or genericized in place.
- **New normative content added** (previously verified-absent by grep): Tier 1 residual-risk security floor in deploy/README (risk keeper P1); raised compose hardening floor (segmented networks, cap_drop/no-new-privileges, mem/pids limits, scoped git PAT, chmod 600); D6 Tier 1 driver mechanics (scoped socket proxy — raw docker.sock forbidden; validator-egress deviation documented); dated supersession note for the stale disposition row (frozen file untouched); M3 floor-inheritance + concentration note (P6).
- **Refutation-found misses all fixed**: OVERVIEW rows 4/8 ("in-cluster" → compose), row 14 Tier-2/3, PVC references (×2), ARCHITECTURE mermaid dangling OQ7 ref, docker/daemon README "pod"/lockstep-pair wording, ROADMAP:28 homelab names (genericized, list → runbook).

## Edit list notes for the applier

- ROADMAP.md is replaced wholesale by `roadmapMarkdown` (do not apply per-edit diffs to it). All probes, gates, acceptance bars, standing rules, and the Parked section are preserved; only "validator-Job pattern" → "validator-sandbox pattern" (accuracy on compose).
- Issue edits: #1 two in-body edits; #3 retitle + full body replace (keeps its immutable number — #1's "(#3)" cross-ref stays valid); #4 retitle + full body replace. #2 verified clean, no edit.
- **Manual steps (outside the edit schema)**: rename GitHub milestone objects — #3 "M3 — Budget enforcement + Kubernetes" → "M4 — Kubernetes (optional, generic)" (the stale budget→K8s coupling lives in this title too), #4 "M4 — Team evolution" → "M3 — Team evolution"; re-map any issue↔milestone assignments accordingly. AGENTS.md is a symlink to CLAUDE.md — the one CLAUDE.md edit covers both.
- Frozen evidence (`docs/VALIDATION-*`, `docs/research/*`) untouched, per standing rule; staleness handled via the dated supersession note in ARCHITECTURE.md.

## Preservation audit (all pass)

v0.1.0 ship gate verbatim; capability-matrix exit rule verbatim; max_budget insurance intact (M1a proof + M2b watchdog); D6 validator trust model intact and strengthened (socket policy + egress deviation now written); license guardrails intact (pin/LICENSE-diff/conformance-suite rules carried, plus the single versions file); session mechanics and verified-absent features respected throughout; idle-pool fallback preserved with the amended precision. Out-of-scope suggestion (one line): an honest QUICKSTART.md placeholder (visitor proposal) would help conversion but was not mandated — left to the operator.