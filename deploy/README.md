# deploy/

- **M1:** docker-compose for development — NATS JetStream, LiteLLM, Postgres.
- **M5:** Helm chart — orchestrator, UI, NATS, LiteLLM, Postgres; agents as Kubernetes Jobs. Topology in `docs/ARCHITECTURE.md`.
