// ExecutionSubstrate adapter over Multica's REST/WS API (PAT auth) — the
// anti-corruption layer of D8. Implemented in M1 against a local instance,
// starting with the API-probe results. See docs/ROADMAP.md.
export {};
