// The Guild conductor: stage planner, approval gate, contract validator,
// budget watchdog — hexagonal per D7, substrate behind the ExecutionSubstrate
// port per D8. Implemented from M1/M2 — see docs/ROADMAP.md.
export {};
